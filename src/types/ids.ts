export type RoomId = string;
export type PlayerId = string;
/** Numeric id used on the wire; 1-based, unique within a room's lifetime. */
export type FishId = number;
/** 1-based seat number: PLAYER 1 .. PLAYER 4. */
export type PlayerNumber = number;
